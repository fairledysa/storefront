import { NextResponse } from "next/server";
import { BootstrapError } from "@/data/mobile/bootstrap/bootstrap.errors";
import { getMobileProduct } from "@/data/mobile/product/product.server";
import { readMobileRequestContext } from "@/data/mobile/request-context";
export const dynamic = "force-dynamic";
const corsHeaders = {"Access-Control-Allow-Origin":"*","Access-Control-Allow-Methods":"GET,OPTIONS","Access-Control-Allow-Headers":"Accept, Content-Type, Authorization, X-Store-App-Id, X-App-Version, X-App-Environment, X-Platform, Accept-Language, X-Timezone, X-Currency-Code, X-Request-Id"};
export async function OPTIONS(){return new Response(null,{status:204,headers:corsHeaders});}
export async function GET(request:Request,context:{params:Promise<{id:string}>}){let requestId=request.headers.get("x-request-id")||"unknown";try{const mobileContext=readMobileRequestContext(request);requestId=mobileContext.requestId;const {id}=await context.params;const payload=await getMobileProduct(mobileContext,id);if(!payload)return NextResponse.json({error:{code:"PRODUCT_NOT_FOUND",message:"Product not found.",requestId}},{status:404,headers:corsHeaders});return NextResponse.json(payload,{headers:{...corsHeaders,"Cache-Control":"private, no-store","X-Config-Version":String(payload.config_version),"X-Request-Id":requestId}});}catch(error){if(error instanceof BootstrapError||(error&&typeof error==="object"&&"status" in error)){const value=error as BootstrapError&{status:number;code:string;publicMessage?:string};return NextResponse.json({error:{code:value.code,message:value.publicMessage??"Invalid mobile request.",requestId}},{status:value.status,headers:corsHeaders});}console.error("mobile product failed",error);return NextResponse.json({error:{code:"MOBILE_PRODUCT_FAILED",message:"Unable to load product.",requestId}},{status:500,headers:corsHeaders});}}
